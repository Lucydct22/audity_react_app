import { createContext } from "react";
import initialStatisticState from "./initialStatisticState";
const StatisticAdminContext = createContext(initialStatisticState)

export default StatisticAdminContext